# 语藏卡片与账号侧栏视觉 QA

- 验收视口：1440 × 900，桌面端。
- 参考图：`artifacts/reference-prompt-card-hover.png`。
- 实现截图：`artifacts/yucang-cards-default.png`、`artifacts/yucang-cards-hover.png`。
- 对比方式：参考图与实现截图合并为同一比较图后逐项检查。

## 验收结果

- 默认卡片只展示完整图片、左上角半透明内容类型和右上角点赞；没有常驻标题或说明文字。
- 悬停后仅卡片底部出现半透明操作层，展示作者、收进 Prompt Vault 和复制操作；右上角点赞位置保持不变。
- 点赞按钮与卡片打开链接是独立点击区域，不会因点击点赞进入详情。
- 桌面端账号侧栏进入独立网格列并压缩主内容宽度，不覆盖主内容；关闭侧栏后主内容恢复全宽。
- 手机窄屏保留覆盖式抽屉，避免将正文压缩到不可用宽度。

最终结果：passed
