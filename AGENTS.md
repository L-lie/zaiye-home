# 再野文化工作室官网协作规则

## 工作方式

- 每一轮只处理一个“当前唯一动作”。
- 开始修改前必须说明：要做什么、会改哪些文件、不会改什么，并等待用户确认。
- 如果需求存在多种理解，先说明分歧并询问，不自行选择。
- 未被当前动作明确涉及的页面、样式、交互、数据和文件不得顺手修改。
- 不以“优化”“统一风格”或“顺便整理”为由改变已经可用的功能或版面。

## 文件职责

- `AGENTS.md`：保存长期稳定的项目规则。
- `CODEX_TASK_STATE.md`：保存当前唯一动作、待办、已验证事实和同步状态。
- Git：保存已经完成并验证的代码事实。
- 临时想法、截图讨论和未确认方案不作为已完成事实写入 Git 提交说明。

## 修改原则

- 修改前先读取现有实现，只做解决当前动作所需的最小改动。
- 每一项改动都必须能直接对应用户确认的当前动作。
- 涉及作品页、笔记、画布或工具时，不改变其他模块的行为。
- 不重构与当前动作无关的代码，不清理原有的无关文件。

## 私人内容与安全

- 不读取、展示、发送或提交 `.private/`、`.env`、密钥、令牌和账号信息。
- 不提交 `node_modules/`、缓存、临时文件、系统生成文件和设计草稿。
- 私人笔记、私人画布、分享密钥和加密源文件必须留在约定的私有范围内。
- 公开站点只提交经过确认的公开内容或加密后的发布文件。

## 验证

- 每次完成动作后，只验证当前动作及其直接影响。
- 页面改动至少检查：本地打开、桌面布局、手机布局和核心点击路径。
- 交互改动必须实际操作验证，不能只检查代码。
- 验证失败时先修复当前动作，不开启新的改动。

## Git 与线上同步

- 提交前查看 Git 状态，只暂存当前唯一动作涉及的文件。
- 发现无关改动、未知文件或私人文件时，停止提交并向用户说明。
- 提交信息必须准确描述已经完成并验证的内容。
- 提交、推送和线上部署是独立动作；执行前必须说明范围并获得用户确认。
- 不得把其他任务线的未提交文件混入当前提交。

## 交接

- 每次结束时更新 `CODEX_TASK_STATE.md`：当前状态、已验证内容、下一唯一动作和同步状态。
- 新对话开始时，先读取 `AGENTS.md`、`CODEX_TASK_STATE.md` 和 Git 状态，再处理任务。

