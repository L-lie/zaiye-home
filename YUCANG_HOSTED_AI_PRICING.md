# 语藏托管 AI 服务内部停用底稿

状态：未启用、未对外展示。官网不提供此页面、导航、充值、付款或购买入口；充值、钱包、扣点和托管 AI 调用均未开放。本文仅保留为未来可能恢复时的内部产品与工程底稿。

语藏托管 AI 面向不想自行配置 API Key 的用户。基础扩展的本地功能永久免费且无需登录；使用用户自己的 API Key（BYOK）时，语藏不收平台 AI 服务费，模型服务商费用由用户承担，Key 仅保存在本机。云同步仍然关闭。

## 1. 服务边界

- 托管 AI 必须登录，并使用预付语藏点。
- 平台 API Key 只保存在服务端，不进入 URL，也不下发到浏览器扩展。
- 一次调用只处理用户明确提交的当前 Prompt 或图片。
- 服务不得扫描、搜索或枚举扩展私库，登录不会自动上传本地 Prompt。
- 当前不实现支付、钱包、真实充值、扣点或模型代理接口。

## 2. 点数与计费算法

换算标准：**100 语藏点 = 人民币 1 元**。

| 模型档位 | 输入价格 | 输出价格 |
| --- | ---: | ---: |
| 经济模型 | 1 点 / 1000 token | 4 点 / 1000 token |
| 品质模型 | 2 点 / 1000 token | 8 点 / 1000 token |

- 每次请求最低扣 2 点。
- 图片由具体模型折算成输入 token 后计费。
- 执行前显示预计点数范围。
- 完成后以模型服务商返回的实际 usage 结算。
- 用量明细必须包含模型、输入 token、输出 token、扣点和时间。
- 第三方或平台失败且没有产生有效模型结果时不扣点。
- 请求必须使用幂等键，避免网络重试造成重复扣点。

计算示例：

- 经济模型，约 2000 输入 token + 1000 输出 token：`2 × 1 + 1 × 4 = 6 点`，约 ¥0.06。
- 品质模型，同样用量：`2 × 2 + 1 × 8 = 12 点`，约 ¥0.12。

实际价格会随上下文长度、图片尺寸和输出长度变化。

## 3. 建议充值档位

以下仅作标准展示，全部标注“即将开放”，页面不得提供付款按钮。

| 档位 | 价格 | 语藏点 |
| --- | ---: | ---: |
| 轻量 | ¥9.9 | 1000 点 |
| 创作者 | ¥29 | 3200 点 |
| 工作室 | ¥69 | 8000 点 |

新登录用户计划在托管 AI 内测开放时一次性获得 100 点体验点；当前不提前发放。

## 4. 余额规则

- 默认不自动续费。
- 不允许透支，余额不足时停止调用。
- 付费点永久有效。
- 赠送点可在发放时明确标注有效期。
- 每笔扣点都必须进入不可变用量账本；修正使用冲正记录，不覆盖历史。

## 5. 成本依据与毛利压力测试

公开测算需要覆盖模型输入与输出成本、汇率、支付通道、服务器、网络、滥用防护、异常重试与运营缓冲。当前研究参考包括：

- Gemini 3.5 Flash-Lite 官方标准价：$0.30 / 1M 输入 token，$2.50 / 1M 输出 token；
- OpenAI GPT-4.1 mini：$0.40 / 1M 输入 token，$1.60 / 1M 输出 token；
- DeepSeek v4 flash：按官方峰谷价格浮动。

这些价格只用于压力测试，不代表语藏已经接入或承诺提供对应模型。正式开放前必须基于实际选定模型、真实请求样本、汇率和服务端账单重新测算。模型或上游价格变化时，应在新标准生效前公示。

毛利压力测试至少需要分别模拟：汇率上升、供应商提价、长输出占比提高、图片输入增加、支付通道费、服务端费用、滥用损耗和失败重试。只有在保守情景下仍能覆盖这些成本，才可以把当前点数标准用于真实充值。

参考来源：

- [DeepSeek 官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)
- [阿里云百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)
- [Supabase Edge Functions 价格](https://supabase.com/docs/guides/functions/pricing)

## 6. 开放门槛

充值和托管 AI 开放前必须完成并验证：

- 合法收款主体与合规支付渠道；
- 服务端订单、签名 webhook、退款、发票与对账；
- 账户限速、IP 限速、单请求 token 上限和全局预算；
- 请求幂等、失败不扣点和响应有效性判断；
- 付费点、赠送点和用量的不可变账本；
- API Key 只存在于服务端密钥环境；
- 用户协议、隐私政策与 AI 服务规则更新完成。

## 7. 对外公示结论

当前仅公布标准，充值与托管 AI 尚未开放。现阶段用户继续使用自己的 API Key。页面 CTA 只允许返回扩展介绍或查看自带 API 配置教程，不得出现付款、充值或伪造余额入口。
