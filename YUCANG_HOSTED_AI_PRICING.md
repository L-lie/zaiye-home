# 语藏托管 AI 服务付费标准

状态：价格方案与开放门槛记录，购买和托管调用尚未开放。

本文件记录语藏托管 AI 服务的成本假设、额度算法、套餐与开放条件。它不是付款协议，也不代表任何模型已经接入。用户继续使用自己的 API Key 时，平台不收取 AI 服务费，Key 仅保存在本机。云同步仍然关闭。

## 1. 服务边界

- 托管服务面向不希望自行配置 API Key 的用户。
- 第一版计划接入标准文本 DeepSeek V4 Flash、进阶推理 DeepSeek V4 Pro、图像理解 Qwen3-VL-Flash。具体名称、可用性和上游单价以正式接入时的供应商公示为准。
- OpenAI、Gemini、Groq 不作为中国区默认托管服务。
- 平台 Key 只保存在服务端，不进入 URL，不下发到浏览器扩展。
- 一次调用只处理用户明确提交的当前 Prompt 或图片。服务不得扫描、搜索或枚举扩展私库，登录也不会自动上传本地 Prompt。

## 2. 成本依据

公开测算需要同时覆盖：模型输入成本、模型输出成本、图像理解额外成本、服务端函数调用、网络流量、支付通道、退款损耗、异常重试和运营缓冲。

上游价格来源：

- [DeepSeek 官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)
- [阿里云百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)
- [Supabase Edge Functions 价格](https://supabase.com/docs/guides/functions/pricing)

正式开放前必须以实际选定模型、真实请求样本和正式服务端账单重新测算。本文件不把尚未正式提供的模型方向描述为当前已有服务。

## 3. 额度算法

1 个 AI 额度等于一个计费块：

- 最多 4,000 输入 token；
- 最多 2,000 输出 token；
- 输入或输出任一方向超出时，按各自块数取较大值并向上取整；
- 标准文本倍率 1x；
- 图像理解倍率 2x；
- 进阶推理倍率 3x。

计算公式：

```text
输入块数 = ceil(实际输入 token / 4000)
输出块数 = ceil(实际输出 token / 2000)
基础块数 = max(1, 输入块数, 输出块数)
实际额度 = 基础块数 × 模型倍率
```

调用前显示预计额度。调用成功后按实际块结算。第三方或平台失败且没有有效输出时不扣额度。必须使用请求幂等键，避免网络重试造成重复扣费。禁止提供无限量套餐。

## 4. 公示套餐

套餐均为一次性购买，不自动续费。购买功能当前禁用。

| 套餐 | 价格 | 额度 | 有效期 |
| --- | ---: | ---: | ---: |
| 体验 | 注册后赠送 | 30 | 30 天 |
| 轻量 | ¥9.9 | 240 | 180 天 |
| 创作者 | ¥29 | 750 | 365 天 |
| 工作室 | ¥69 | 1800 | 365 天 |

价格调整只影响以后新购买，不追溯已经购买的额度。赠送额度不折现。

## 5. 毛利压力测试

下表只用于判断套餐是否有足够安全空间。假设每个已结算基础额度已经包含模型、函数、网络和异常缓冲的平均成本，不包含税费、支付通道费、人工客服和退款操作成本。

| 套餐 | 单额度收入 | 成本 ¥0.008 时毛利率 | 成本 ¥0.015 时毛利率 | 成本 ¥0.025 时毛利率 |
| --- | ---: | ---: | ---: | ---: |
| 轻量 | ¥0.04125 | 80.6% | 63.6% | 39.4% |
| 创作者 | ¥0.03867 | 79.3% | 61.2% | 35.3% |
| 工作室 | ¥0.03833 | 79.1% | 60.9% | 34.8% |

压力测试结论：若含基础设施后的平均成本接近 ¥0.025/额度，套餐已经难以覆盖支付、税务、退款和运营成本。正式开放门槛应要求按真实调用分布验证平均成本，并设置全局预算与供应商价格变动告警。体验额度全部视为平台获客成本。

## 6. 退款公示

- 购买后 7 天内且未使用，可申请全额原路退款。
- 已经使用后，按实际消费的付费额度扣除，退还未使用的付费部分。
- 赠送额度不折现。
- 服务终止或平台无法继续提供时，退还未使用的付费部分。

退款计算必须区分赠送额度与付费额度，并保留订单、消费、退款和对账记录。

## 7. 支付开放门槛

购买开放前必须全部完成并验证：

### 业务与合规

- 明确可收款的商户主体；
- 接入支付宝、微信支付或合规收单服务；
- 明确订单、退款、发票和财务对账流程；
- 更新用户协议、隐私政策、AI 服务规则和退款规则；
- 明确第三方模型许可、内容治理和服务终止处理。

### 支付与账务

- 服务端创建订单，不信任前端金额；
- 使用签名 webhook 确认付款结果；
- webhook 幂等和重放保护；
- 额度发放与订单状态使用受控事务；
- 不可变用量账本，任何调整使用冲正记录而不是覆盖历史；
- 退款、发票、日结和差异对账可追溯。

### 安全与成本控制

- 服务端账户限速与 IP 限速；
- 单请求输入、输出 token 上限；
- 全局预算与按日熔断；
- 请求幂等、超时和失败不扣费规则；
- 模型响应有效性判定；
- API Key 只在服务端密钥环境中使用；
- 压测、异常重试测试和用量账本一致性测试通过。

## 8. OpenAI 集成边界

OpenAI API 可以集成到 Customer Application，并以应用内功能提供给终端用户。不能转售、转让或共享 OpenAI 账号、访问凭据或 API Key。

本方案未来出售的是语藏应用内的托管 AI 功能和可计量使用额度，不出售账号，不转售 API Key，也不会把平台 Key 下发给用户或扩展。若未来接入 OpenAI，必须再次核对届时有效的服务条款和适用地区要求。

