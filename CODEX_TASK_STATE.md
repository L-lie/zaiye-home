# 当前任务状态

## 2026-09-03：免费社区 MVP 必做项补齐

- 已按 `YUCANG_PROMPT_COMMUNITY_MVP_PRD.md` 的阶段 1 清单复核并补齐网站缺口；阶段 1.5、交易和论坛等后续范围未提前混入。
- 提示词库已提供图像、视频、文字与办公、编程分类路由和搜索结果路由；搜索覆盖标题、用途、模型、标签与创作者昵称。
- 已新增网站原生“我的收藏”、账号与隐私页、创作者简介和公开主页开关；按最新产品决定不提供创作者聚合页，单个创作者主页仍可从作品作者信息和“我的”侧栏进入。
- 作品详情已补齐负面 Prompt、生成参数、第三方依赖、使用说明、标签、作者来源、结构化授权范围，以及可访问的历史公开版本。
- 提示词库卡片按图片自身比例展示，不做统一裁切；已从 Prompt Vault 本地导出恢复“暗黑哥特风”1200×2133 和“粒子海报”607×1080 原图，其余早期截图裁切图仍待对应导出原图替换。
- 首页无保存偏好时默认夜间，其他路由默认日间；用户手动选择仍会被尊重和保存。
- 已验证 1280×720 桌面和 390×844 手机布局；分类在手机端自动换行，详情操作区不再产生横向滚动。
- 数据库迁移 `20260903000300_yucang_mvp_library_accounts.sql` 已部署，包含原生收藏、公开范围、创作者聚合和公开历史版本能力。
- 20 项语藏自动测试、JavaScript 语法和差异检查通过。真实多账号角色回归仍需可用的普通用户和管理员测试账号，不创建或读取生产账号作为替代。
- 本次只修改网站和网站数据库，没有修改 Prompt Vault 扩展目录。

## 2026-09-03：创作者主页入口与页面返回导航

- “我的”侧栏已新增“查看我的公开主页”，不再依赖先找到一条公开作品才能进入创作者主页。
- 已登录的创作者本人即使还没有公开作品，也可以查看自己的空主页；其他访客仍只能看到至少有一条公开作品的创作者。
- “我的发布”、网站新建 Prompt、举报与申诉、治理后台和审核详情已新增明确的“返回首页 / 提示词库”页面导航。
- 侧栏右上角“×”用于关闭侧栏，底部“退出当前账号”用于退出登录，两者含义保持分开。
- 数据库迁移 `20260903000200_yucang_creator_self_profile.sql` 已部署到 Supabase。
- 19 项语藏测试、JavaScript 语法和差异检查全部通过。

## 2026-09-03：创作者主页、首页真实点赞与路由默认主题

- 首页在用户没有保存主题偏好时默认使用夜间模式；提示词库、作品详情、创作者主页和其他路由默认使用日间模式。
- 用户手动切换主题后会保存选择，跨路由继续使用该选择，不再被页面默认值覆盖。
- 首页 12 张首发精选卡片已读取 `yucang_get_like_counts` 的真实点赞数，不再使用仅供展示的固定数值。
- 已新增 `#/creator/{slug}` 创作者公开主页，作品卡片和作品详情中的作者名称可以进入主页。
- 公开作者 RPC 对访客只返回至少拥有一条当前公开作品的作者；已登录本人可以预览自己的空主页。接口提供公开昵称、头像、作品数和最近发布时间。
- 数据库迁移 `20260903000100_yucang_public_creator_profiles.sql` 已部署到 Supabase。
- 19 项语藏测试全部通过；桌面与 390×844 手机下已验证默认主题、手动主题保留和无横向溢出。
- 当前线上尚无用户公开作品；本人空主页已开放，访客查看不存在或尚未发布作品的其他创作者仍返回空结果。
- 网站后续待办：网站原生收藏夹、账号公开范围控制，以及发布者、普通用户和管理员的真实多账号端到端回归。

## 2026-09-03：语藏社区举报、申诉与规则

- 网站已新增作品、评论、公开账号和站方资源的举报入口；游客举报需填写联系邮箱，登录用户可直接提交。
- 已新增“我的举报与申诉”中心，用户可以查看处理状态，并对自己的下架作品、隐藏评论或账号警告提交申诉。
- 管理后台已新增举报与申诉队列，支持审核中、驳回、无需处置、下架作品、隐藏评论、账号警告，以及申诉维持或恢复。
- 数据库迁移 `20260902000200_yucang_governance_reports_appeals.sql` 已部署到 Supabase；举报、处置和申诉均保留审计记录，公开内容不会因举报自动下架。
- 已新增 `yucang/rules.html`，覆盖公开发布、免费授权、举报处理、知识产权投诉与反通知、申诉复核和信息留存。
- 已完成桌面与 390×844 手机验证：举报弹窗按钮完整可见，页面与规则页无横向溢出；18 项语藏测试全部通过。
- 本任务只修改网站与网站数据库，没有修改 Prompt Vault 扩展目录；扩展工作继续由专门的扩展任务负责。
- 本节网站代码已在提交 `5d55068` 推送到 `main`；后续待办已更新到上方最新状态。

## 2026-09-02：Prompt Vault 账号私有备份与首页主题修复

- 当前网站工作树：`E:\work\3.工作室\llie-studio-site-backlog`，分支 `main`；本节网站代码已在提交 `9916450` 部署。
- 首页文字人形已按主题切换：浅色背景使用深灰文字粒子，深色背景保留浅色文字粒子；桌面浅/深色与 390×844 手机浅色已实际验证，无横向溢出。
- 已新增 `prompt_vault_account_backups` 私有备份表及受控读取/写入 RPC；迁移 `202609020001_prompt_vault_account_backup.sql` 已部署到 Supabase，匿名调用实测返回 401。
- 隐私政策已更新，明确登录后的账号私有 Prompt 备份范围、API Key/密码/令牌排除、非公开边界、删除与恢复规则。
- 网站测试通过：语法检查、Yucang Slice 1、扩展登录契约、发布控制。
- 后续扩展打包与真实登录、上传、清空本机、恢复回归已交给专门的扩展任务处理，避免与网站开发冲突。
- 同步状态：数据库迁移和网站代码均已上线；本网站任务不维护扩展版本状态。

最后更新：2026-07-26
仓库：`E:\work\3.工作室\llie-studio-site`

## 当前唯一动作

状态：已完成并验证，随本次提交同步官网

动作：上线 Supabase 权限底座、项目交接文件和已完成的重叠切图工具改动。

## 已验证的 Git 状态

- 当前分支：`main`
- 当前 HEAD：`5f56afa Add Supabase access control foundation`
- 推送前状态：`main` 比 `origin/main` 多 1 个 Supabase 权限基础提交
- 本次新增并验证的 3 个工具文件：
  - `tools/overlap-slicer/editor.css`
  - `tools/overlap-slicer/editor.js`
  - `tools/overlap-slicer/index.html`
- 上述工具改动与交接文件分别提交，不与 Supabase 权限基础混写提交历史。

## 任务线一：网站账户与权限

### 已完成

- 已建立 Supabase 数据库与权限基础。
- 已新增 `.env.example`、`SUPABASE_SETUP.md` 和 `supabase/schema.sql`。
- 已在 `.gitignore` 中加入本地环境变量忽略规则。
- 权限模型区分：公开访客、普通注册用户、站点所有者和分享链接访问者。

### 尚未完成

- 尚未创建真正的登录界面。
- 尚未把站点所有者账号设置为 `owner`。
- 尚未实现登录后的“编辑模式”。
- 尚未把主页、作品、私人笔记和私人画布接入 Supabase。
- 尚未实现分享链接、密码、到期时间和撤销机制。

## 任务线二：重叠切图工具

### 已完成并验证

- 添加“1 格”预设按钮。
- 初始空页面改为紧凑高度，左右面板不再互相拉伸。
- 空切片列表不占据页面空间。
- 暴露 `window.OverlapSlicerLoadSource`，供浏览器扩展直接载入图片。
- CSS 和 JS 引用添加版本号，避免线上页面继续使用旧缓存。
- 已有图片时可导入新图片直接替换当前图片。
- 被替换的原图进入“历史原图”，可以来回切换。
- 新增“清图”按钮，清空当前原图时保留历史。
- 多次自由裁切会把结果持续追加到右侧，不再覆盖前一次结果。
- 已在本地页面实际验证：替换图片、历史切换、“1 格”、两次裁剪累积和清图。
- 已修复历史列表把旧图误标成新文件名的问题。

## 当前工作边界

- 不读取或提交 `.private/`、`.env`、密钥、令牌和账号信息。
- 不把账户权限任务与重叠切图工具的改动混在同一个提交里。
- 未经用户确认，不修改页面、不提交、不推送、不部署。

## 下一唯一动作

创建并配置 Supabase 项目，然后接入网站登录、`owner` 身份和编辑模式。

登录接通后，再处理作品页美术资料文字：

- 作品名保持当前粗体样式并移动到文字行最左侧。
- 图片介绍与制作方式使用灰色正常字重，放在作品名右侧。
- `owner` 登录并开启编辑模式后，可以修改并保存这些文字。
